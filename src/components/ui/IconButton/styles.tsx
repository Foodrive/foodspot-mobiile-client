import { StyleSheet } from "react-native";
import { colors, monstserrat } from "@app/utils";

const styles = StyleSheet.create({ 
  iconbutton: {
    backgroundColor: "#FFA500"
  }
});

export default styles; 